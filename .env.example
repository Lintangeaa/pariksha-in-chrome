# Backend base URL the extension talks to (auth login, PB list, session upload).
# Inlined into the bundle at build time (see esbuild.config.mjs) since a
# service worker/content script cannot read process.env at runtime.
PARIKSHA_BACKEND_URL="http://localhost:3000"
